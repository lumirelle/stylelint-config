name: Release Commit

on:
  push:
    branches: ['**']
    tags-ignore: ['v*']
  pull_request: {}

permissions: {}

jobs:
  release-commit:
    uses: lumirelle/workflows/.github/workflows/release-commit.yml@v4
    with:
      compact: true
