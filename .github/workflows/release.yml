name: Release

on:
  push:
    tags: ['v*']

permissions: {}

jobs:
  release:
    uses: lumirelle/workflows/.github/workflows/release.yml@v3
    with:
      publish: true
      packages: './packages/*'
    permissions:
      contents: write
      id-token: write
